// import { ref, list, getDownloadURL } from 'firebase/storage'
// import { storage } from 'lib/services/firebase/client'

export const getBanner = async () => {
  // const bannerStoageListRef = await list(ref(storage, '/banner'))
  // const promiseBannerList = bannerStoageListRef.items.map(async (banner) => {
  //   const imageURL = await getDownloadURL(ref(storage, banner.fullPath))
  //   return imageURL
  // })

  // const bannerList = await Promise.all(promiseBannerList)
  //   .then(results => results)

  // return bannerList
  return []
}
